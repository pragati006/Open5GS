# Route ALSA's default PCM + control through PulseAudio.
# baresip's alsa module then reaches WSLg's PulseAudio (your laptop mic/speaker).
pcm.!default {
    type pulse
}
ctl.!default {
    type pulse
}
