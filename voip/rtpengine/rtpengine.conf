[rtpengine]
# MY_IP is replaced by the container entrypoint with the detected interface IP.
# We override it via the MY_IP env var to pin to the data-net address.
listen-ng=MY_IP:22222
interface=MY_IP
port-min=30000
port-max=31000
log-level=5
log-stderr=true
no-fallback=true
